/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind CSS v4 configuration
  // Most configuration is now done in CSS using @theme directive
  // This file can be used for plugins and other JS-based configuration
};